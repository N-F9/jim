JIM

Abstraction: Jim provides tools that the Ti-84 Plus CE does not, like solving triangles. Jim will not have the funcationality to solve polynomials (use the polynomial root finder from Ti), for example.